---
    import { SCOPE_API_ENDPOINT } from "astro:env/server";

    const response = await fetch(SCOPE_API_ENDPOINT)
    const scoreValue = await response.text();
---

<span class="text-xs" id="score">
    ⭐ Puntuación: {scoreValue.trim()}/5
</span>
